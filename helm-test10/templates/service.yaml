apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-test10.fullname" . }}
  labels:
    {{- include "helm-test10.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-test10.selectorLabels" . | nindent 4 }}
